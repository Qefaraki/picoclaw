# Binaries
# Go build artifacts
bin/
build/
*.exe
*.dll
*.so
*.dylib
*.test
*.out
/picoclaw
/picoclaw-test
cmd/picoclaw/workspace

# Picoclaw specific

# PicoClaw
.picoclaw/
# Root-level config.json only (config/config.json is tracked for deployment)
/config.json
sessions/
build/

# Coverage

# Secrets & Config (keep templates, ignore actual secrets)
.env
# config/config.json is tracked — it contains NO secrets (env vars inject secrets at runtime)

# Test
coverage.txt
coverage.html

# OS
.DS_Store

# Ralph workspace
ralph/
.ralph/
tasks/

# Editors
.vscode/
.idea/

# Added by goreleaser init:
dist/
